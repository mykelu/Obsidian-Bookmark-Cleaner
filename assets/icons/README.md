# Icons Placeholder

Placeholder directory for extension icons. 

To use real icons, add the following files to this directory and uncomment/add the `icons` key in `manifest.json`:
- `icon16.png`
- `icon48.png`
- `icon128.png`
